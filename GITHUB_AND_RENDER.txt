STOCKSTUNERSTATION — GitHub + Render (you are here)
=================================================

DONE ON YOUR PC:
  - Git repository created in this folder
  - All app code committed on branch "main"
  - Render startup fix included (health check)

YOU DO 2 THINGS (about 5 minutes total):


PART 1 — PUBLISH TO GITHUB (GitHub Desktop)
-------------------------------------------

FASTEST: Double-click COMPLETE_GITHUB_SETUP.bat in this folder.
   (Opens GitHub Desktop, then pushes + opens Render after you publish.)

OR manual:

1. Double-click COMPLETE_GITHUB_SETUP.bat  OR  PUBLISH_TO_GITHUB.bat
   (Opens GitHub Desktop on this folder automatically)

2. Sign in with your GitHub account

3. If it says "not a Git repository", click "create a repository"

4. Click the blue button: "Publish repository"

7. Name: stock-screener-Tuner  (or any name — remember it for Render)
   Uncheck "Keep this code private" (optional, for easier Render)

8. Click "Publish repository"

9. Your code should be on github.com (e.g. uniqueafsuraf-sketch/stock-screener-Tuner)


PART 2 — REDEPLOY ON RENDER
---------------------------

IMPORTANT: If Render says "repository is empty", open FIX_RENDER_EMPTY_REPO.md
Render must connect to the repo that HAS your code (stock-screener-Tuner), not an empty one.

1. Go to https://dashboard.render.com

2. Open your "stockstunerstation" service
   Settings → Repository → connect: stock-screener-Tuner, branch main

3. Settings — check:
   Root Directory:  (leave EMPTY)
   Build Command:   pip install -r requirements.txt
   Start Command:   gunicorn wsgi:application --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 180
   Branch:          main

4. Click "Manual Deploy" → "Deploy latest commit"

5. Wait until status is LIVE (green) — 5-10 minutes

6. Test:
   https://stockstunerstation.onrender.com/api/health
   (should show JSON with "ok": true)

7. Open:
   https://stockstunerstation.onrender.com


TROUBLESHOOTING
---------------
See RENDER_FIX.md if the site still says "not found".

Quick test on PC while waiting:
  Double-click START.bat
